import React from 'react';
import ProjectsWraper from '../components/ProjectsWraper';

const AllProjects = () => {
    return (
        <main className='all-projects'>
            <h2>Todos mis proyectos</h2>
            <ProjectsWraper />
        </main>
    );
}

export default AllProjects;
